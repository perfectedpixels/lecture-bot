# Used by Streamlit Community Cloud when the main file is under app/
# (Cloud resolves requirements from the entrypoint directory BEFORE repo root.)
# Keep in sync with /requirements.txt at repo root.
streamlit>=1.28.0
boto3>=1.28.0
plotly>=5.0.0
pandas>=1.0.0
anthropic>=0.3.0
elevenlabs>=0.2.0
striprtf>=0.0.26
requests>=2.31.0
